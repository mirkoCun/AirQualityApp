How to run?
Clone the Repository: Download the project to a local environment like XAMPP or WAMP.
The Database: Open takeDataApi.php in your broswer and the system will automatically create and populate the database with the data from Open-Meteo’s API
Launch: Open index.php in your browser via localhost. The system will automatically start fetching data if the API script is triggered.
